apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "visual.fullname" . }}-test-connection"
  labels:
    {{- include "visual.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "visual.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
